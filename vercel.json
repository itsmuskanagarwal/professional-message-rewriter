{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "cd .. && pnpm build --filter @how-to-talk-corporate/web",
  "installCommand": "pnpm install",
  "outputDirectory": ".next",
  "ignoreCommand": "git log --oneline -1 2>/dev/null | grep -q . && git diff --quiet HEAD^ HEAD -- apps/web/ packages/agents/ pnpm-lock.yaml || exit 1",
  "github": {
    "silent": true
  }
}
